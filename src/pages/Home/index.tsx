import ColorBlock from '../../components/ColorBlock'

const HomePage = (): JSX.Element => (
  <div>
    Home
    <ColorBlock width={100} height={100} color="teal" />
  </div>
)

export default HomePage
